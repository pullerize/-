# Backend API

The backend is built with FastAPI. After running the server, API docs are
available at `/docs` (Swagger UI).

## Useful commands

```bash
uvicorn agency_backend.app.main:app --reload
```

This starts the development server.

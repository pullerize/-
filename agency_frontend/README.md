# Frontend

This frontend is created with Vite + React + TypeScript.

## Development

```bash
npm install
npm run dev
```

The frontend expects the backend to expose the GPT endpoint at `/gpt`. Configure
`VITE_API_URL` in a `.env` file if your backend runs on a different host.
